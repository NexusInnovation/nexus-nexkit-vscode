{
    "name": "nexkit-badge-service",
    "version": "1.0.0",
    "description": "Azure Functions badge service for nexus-nexkit-vscode private repository",
    "main": "dist/src/functions/*.js",
    "scripts": {
        "build": "tsc",
        "watch": "tsc --watch",
        "clean": "rimraf dist",
        "prestart": "npm run clean && npm run build",
        "start": "func start",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@azure/functions": "^4.5.0",
        "badge-maker": "^3.3.1"
    },
    "devDependencies": {
        "@types/node": "^20.11.0",
        "azure-functions-core-tools": "^4.0.5530",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}