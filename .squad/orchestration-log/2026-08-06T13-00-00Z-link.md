# Orchestration Log — Link — 2026-08-06

**Agent routed:** Link (TypeScript & VS Code Extension Dev)
**Mode:** background
**Why chosen:** Owns `commitMessageService.ts` and the commit menu contribution from prior commit-management work.

## Task

Link the existing `nexus-nexkit-vscode.createBranchFromWorkItem` command into the SCM "Commit" submenu, and add a
protected-branch (`main`/`develop`) prompt after generating a commit message that offers to run branch creation.

## Files authorized to read

`package.json`, `src/features/commit-management/commitMessageService.ts`,
`test/suite/commitMessageService.integration.test.ts`, `decisions.md`.

## Files produced

- `package.json` — added `nexus-nexkit-vscode.createBranchFromWorkItem` to `commitMenu` submenu, group `"1_generate@2"`.
- `src/features/commit-management/commitMessageService.ts` — added `PROTECTED_BRANCHES` check + warning prompt after
  commit message generation on `main`/`develop`.
- `test/suite/commitMessageService.integration.test.ts` — 4 new test cases.

## Outcome

`npm run check:types` clean; `npm test` → 444 passing, 0 failing. Decision recorded in `decisions.md`.
