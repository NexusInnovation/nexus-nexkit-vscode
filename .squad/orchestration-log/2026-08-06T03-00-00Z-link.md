# Orchestration Log — Link — 2026-08-06

**Agent routed:** Link (TypeScript & VS Code Extension Dev)
**Mode:** background
**Why chosen:** Owns `branchNameBuilder.ts` from the original DevOps Branch Creation implementation.

## Task

Map Azure DevOps work item types to industry-standard branch prefixes (`bugfix/`, `feature/`, `chore/`,
`experiment/`, `test/`, `docs/`) in `buildBranchName()`, instead of always slugifying the raw work item type.
Case-insensitive lookup with fallback to the original slugify behavior for unmapped/custom types.

## Files authorized to read

`src/features/devops-branch-creation/branchNameBuilder.ts`, associated tests, `decisions.md`.

## Files produced

- `src/features/devops-branch-creation/branchNameBuilder.ts` — added work-item-type → prefix mapping table + lookup
- `test/suite/branchNameBuilder.test.ts` — updated
- `test/suite/devOpsBranchCreationService.test.ts` — updated

## Outcome

`npm run check:types` clean; `npm test` → 440 passing, 8 pending, exit code 0. Decision recorded in `decisions.md`.
