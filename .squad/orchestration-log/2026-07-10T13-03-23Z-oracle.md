# Orchestration Log: Oracle — 2026-07-10T13:03:23Z

## Deliverable Summary

**Feature:** Triage of 4 new NexKit Evolution tool proposals (JSON Formatter, RTF→Markdown, Cron Schedule Builder, RegEx Builder) for the panel-ui feature
**Status:** ✅ Complete
**Requestor:** Eric De Carufel

## Runs

### Pass 1 — Research & Clarification (background→sync)

Researched GitHub Project "NexKit Evolution Project" (#5, org NexusInnovation). Drafted 10 clarifying questions covering architecture (unified vs. separate panels), implementation approach (bundled libraries vs. iframes), priority ranking, and per-tool technical scope. Presented questions to Eric via the ask-questions tool.

### Pass 2 — Blocked on `gh` CLI scopes

Initial issue-creation attempt was blocked by missing `gh` CLI `project` / `read:project` OAuth scopes. Eric ran `gh auth refresh -s project -s read:project` to unblock.

### Pass 3 — Issue Creation & Triage

Created 4 GitHub issues in `NexusInnovation/nexus-nexkit-vscode`, labeled `squad`:
- #175 — JSON Formatter
- #176 — RTF to Markdown
- #177 — Cron Job Schedule Builder
- #178 — RegEx Builder

Added all 4 to Project #5 (project ID `PVT_kwDOCHE2jM4BGWXr`) for triage.

## Key Decisions (from Eric's answers)

- 4 tools = 4 separate panels/commands, not a unified tabbed "Dev Tools" webview
- Bundle npm libraries locally; no iframes to external hosted tools (privacy/telemetry/framing concerns)
- No priority ranking among the 4 tools
- Project #5 is independent of the 125-hour Nethris budget — no hour/effort tracking
- JSON Formatter: Monaco Editor + jsonc-parser; line/column validation errors, JSON5/JSONC support, copy + save-to-file buttons; no diff view
- RTF to Markdown: Turndown (HTML paste) + Mammoth.js (.docx) + possibly rtf.js (.rtf) — risk: pure RTF parsers are weak; paste and file upload both required
- Cron Builder: cronstrue + cron-parser + react-js-cron — risk: webview is Preact not React, needs compatibility check; needs NL description, 5/6-field formats, presets
- RegEx Builder: custom panel required (no iframe-embeddable tool works due to X-Frame-Options); JS + .NET regex flavor support — risk: flavor differences need a toggle; needs highlighting, replace preview, common pattern library

## Outcomes

4 issues (#175–#178) created and added to Project #5 for role/effort triage. Decision recorded in `.squad/decisions.md`. Cross-agent note appended to Morpheus (Lead) history for role assignment follow-up.
