# Orchestration Log — Link

**Timestamp:** 2026-07-23T18:00:00Z
**Agent:** Link
**Mode:** sync (general-purpose)
**Requested by:** Eric Decarufel

## Why routed

Investigate and fix a production-only bug: the "Convert to Markdown" webview showed the generic fallback error ("Unable to load Convert to Markdown / Please rebuild the extension and try again.") in installed/packaged builds only. Link owns `src/features/*` service implementations and the build pipeline, making them the correct owner for both the packaging root-cause analysis and the fix.

## Files authorized to read/modify

- `src/features/convert-to-markdown/**`
- `esbuild.config.js`
- `out/**` (build verification only)

## Files produced / modified

- `src/features/convert-to-markdown/webview/index.html` — recreated (missing since the `rtf-converter` → `convert-to-markdown` rename; never regenerated because `esbuild`'s static-file copy silently no-ops on a missing source).
- `esbuild.config.js` — `copyStaticFiles()` now `console.warn`s when a configured source file is missing, instead of silently skipping.
- `src/features/convert-to-markdown/convertToMarkdownPanelService.ts` — `buildWebviewHtml()`'s catch block now logs the real filesystem error via `LoggingService` before falling back to the generic error page.

## Outcome

**Success.** Root cause confirmed via `git log --all` (zero history for the new-path `index.html`) and reproduced with a clean `out/` + `npm run build:ci` (file was missing, no warning emitted pre-fix). Fix verified: clean-checkout rebuild now produces `out/convert-to-markdown/index.html` with no missing-source warning. `npm run check:types` clean. `npm run test-compile && npm run test:unit` → 384 passing, 0 failing.

Decision recorded in `.squad/decisions.md` ("Convert to Markdown — production packaging bug (missing webview index.html)"). Learnings appended directly to `.squad/agents/link/history.md` by Link.

## Notes

Source code changes (`esbuild.config.js`, `src/features/convert-to-markdown/**`) were intentionally left unstaged for Eric to review and commit himself. Only `.squad/` bookkeeping files were staged/committed by the Scribe.
