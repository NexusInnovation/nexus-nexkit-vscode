# Orchestration Log — Link

**Timestamp:** 2026-07-30T18:28:46Z
**Agent:** Link (TypeScript & VS Code Extension Dev)
**Requested by:** Eric Decarufel
**Mode:** sync
**Slice:** ConfirmationService fail-closed consent fix (follow-up)

## Why this agent

The change is a behavioural fix to a shared TypeScript service in a VS Code extension, plus its regression tests — squarely Link's domain. The work was a deferred open item raised by Morpheus during the prerequisite-automation code review and released as its own slice by Eric, precisely so the shared-service blast radius would be visible in review rather than buried inside a feature changeset.

Link was **not** under reviewer lockout for this slice. The prior lockout applied to Morpheus's review of Link's prerequisite-automation implementation, which Ghost revised; this is a separate artifact.

## Input artifacts

- `src/features/.../confirmationService.ts` (the service under change)
- `test/suite/confirmationService.test.ts`
- All five consent call sites (read for audit):
  - `src/features/initialization/mcpConfigDeployer.ts`
  - `src/features/mcp-management/mcpConfigService.ts`
  - `src/features/prerequisite-automation/prerequisiteOrchestratorService.ts`
- `.squad/decisions.md` — the open item raised by Morpheus
- `.squad/decisions-archive.md` — Neo's superseded Issue #162 UX contract

## Files produced

- Modified: `ConfirmationService.confirm()` — explicit `=== "Accept"` accept branch, refusal as default
- Modified: `test/suite/confirmationService.test.ts` — one test removed, three added
- Written: `.squad/decisions/inbox/link-confirmation-service-fail-closed.md`

## Outcome

**SUCCESS.**

- Type-check exit 0
- Lint exit 0
- Test compile exit 0
- Full suite **536 passing / 15 pending / 0 failing** (baseline 534 / 15 / 0)

Net +2 tests: the removed test asserted the fail-open behaviour as intended, and was replaced by three regression tests.

## Notable findings

1. **The suite was green because of the bug.** `"Should return 'accepted' when user dismisses the dialog (ESC)"` encoded the vulnerability as the expectation. Fixing the defect required deleting a passing test — recorded so the removal is not misread as a coverage regression.
2. **All five call sites gate a write or an execution.** There is no caller for which fail-closed is wrong; none loses a capability. `addUserMCPServer()` (machine-global config write) carries the broadest blast radius.
3. **Public signature deliberately unchanged.** A `"dismissed"` union member was considered and rejected — no consumer distinguishes it, and adding it would force edits outside the slice boundary.

## Deferred — routed to Morpheus

- Callers 1–3 fail **silently** on refusal (bare `return`, no user feedback). Pre-existing and shared with the explicit-Refuse path; fixing it changes Refuse-path behaviour too, so it needs its own slice.
- `MCPConfigService.promptInstallRequiredMCPsOnActivation()` calls `showInformationMessage` directly, bypassing `ConfirmationService`. Fails closed today so no live bug, but it will **not** surface in a `.confirm(` grep and should be folded into the service if that dialog ever gains consequences.
